# Plan 2: SEO — Dynamic OG Images, Structured Data, Metadata

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Maximize search visibility and social sharing with dynamic OG images, rich structured data, and complete metadata coverage across all 206 pages.

**Architecture:** OG images use Next.js `ImageResponse` (Satori). Structured data via JSON-LD in layouts. Metadata audit fills gaps in PAGE_META.

**Tech Stack:** Next.js `ImageResponse` (@vercel/og), JSON-LD, next-intl, existing PAGE_META system.

---

### Task 1: Default OG Image Fallback

**Files:**
- Modify: `src/app/opengraph-image.tsx` (may exist, create if not)

- [ ] **Step 1:** Create/update `src/app/opengraph-image.tsx` using Next.js `ImageResponse`. Dark navy (#0a0e27) background, gold (#d4a853) text. Content: "Dekho Panchang" title + "Vedic Astrology — Panchang, Kundali, Muhurta" subtitle. Size: 1200x630.

- [ ] **Step 2:** Create `src/app/twitter-image.tsx` — same content, links to the same generation. Add `twitter:card = summary_large_image` metadata in the root layout.

- [ ] **Step 3:** Verify by visiting `/opengraph-image` in the browser — should render the image.

- [ ] **Step 4:** Commit: `feat: default OG + Twitter image for social sharing`

---

### Task 2: Dynamic Panchang OG Image

**Files:**
- Create: `src/app/[locale]/panchang/opengraph-image.tsx`

- [ ] **Step 1:** Create the OG image route. It computes today's panchang (lightweight — tithi, nakshatra, yoga only via `calculateTithi`, `calculateYoga`, `getNakshatraNumber` from astronomical.ts). Renders: "Today's Panchang — [Month Day, Year]" + tithi name + nakshatra name + yoga name on the branded dark template.

- [ ] **Step 2:** Add `export const revalidate = 3600` (1 hour) so the image updates throughout the day.

- [ ] **Step 3:** Commit: `feat: dynamic panchang OG image with today's tithi/nakshatra`

---

### Task 3: Static OG Images for Kundali, Learn, Festivals

**Files:**
- Create: `src/app/[locale]/kundali/opengraph-image.tsx`
- Create: `src/app/[locale]/learn/opengraph-image.tsx`
- Create: `src/app/[locale]/calendar/festivals/[slug]/opengraph-image.tsx`

- [ ] **Step 1:** Kundali OG: "Generate Your Vedic Birth Chart — Free Kundali with Dashas, Yogas & AI Reading". Static.

- [ ] **Step 2:** Learn OG: "Learn Vedic Astrology — 90 Modules, 24 Courses". Static.

- [ ] **Step 3:** Festival OG: Read slug param, look up festival name + date from festival-defs. Render festival name + "2026" + date on branded template. `revalidate = 86400` (24h).

- [ ] **Step 4:** Commit: `feat: OG images for kundali, learn, and festival pages`

---

### Task 4: Structured Data (JSON-LD)

**Files:**
- Modify: `src/app/[locale]/layout.tsx` (BreadcrumbList)
- Create: `src/lib/seo/structured-data.ts` (generators)
- Modify: Various layout.tsx files for HowTo, Event schemas

- [ ] **Step 1:** Create `src/lib/seo/structured-data.ts` with helper functions:
  - `generateBreadcrumbLD(pathname: string, locale: string)` — auto-generates BreadcrumbList from route path with display name mapping (e.g., "learn/modules/17-3" → "Learn > Kundali Reading > Module 3")
  - `generateHowToLD(title: string, steps: string[])` — for puja vidhi pages
  - `generateEventLD(name: string, date: string, description: string)` — for festival pages

- [ ] **Step 2:** Add BreadcrumbList JSON-LD to the root `[locale]/layout.tsx` — computed from the current pathname. Use a `DISPLAY_NAMES` mapping for route segments.

- [ ] **Step 3:** Add HowTo JSON-LD to puja vidhi layout (`src/app/[locale]/puja/[slug]/layout.tsx` or equivalent). Extract steps from the puja procedure content.

- [ ] **Step 4:** Add Event JSON-LD to festival pages. Use festival date from the calendar data.

- [ ] **Step 5:** Commit: `feat: structured data — BreadcrumbList, HowTo, Event schemas`

---

### Task 5: Metadata Audit & Hreflang

**Files:**
- Modify: `src/lib/seo/metadata.ts`
- Modify: `src/app/[locale]/layout.tsx`

- [ ] **Step 1:** Audit PAGE_META coverage. List all routes under `src/app/[locale]/` that have `page.tsx`. Cross-reference with `PAGE_META` entries. Add missing entries with auto-generated titles/descriptions following the pattern: "[Page Name] — Dekho Panchang | Vedic Astrology".

- [ ] **Step 2:** Add `alternates.canonical` to the `generateMetadata` function in the root layout. Canonical should point to the `en` locale version of each page.

- [ ] **Step 3:** Add `alternates.languages` with hreflang entries for `en`, `hi`, `sa`, and `x-default` (pointing to `en`) in the root layout's metadata.

- [ ] **Step 4:** Commit: `feat: complete metadata coverage + canonical URLs + hreflang tags`

---

### Task 6: Build & Verify

- [ ] **Step 1:** `npx next build` — 0 errors.
- [ ] **Step 2:** Visit `/opengraph-image`, `/en/panchang/opengraph-image`, `/en/kundali/opengraph-image` in browser — verify images render correctly.
- [ ] **Step 3:** Use https://www.opengraph.xyz/ or Facebook sharing debugger to test a few URLs.
- [ ] **Step 4:** Validate structured data at https://validator.schema.org/ for a puja page and a festival page.
- [ ] **Step 5:** Push all changes.
